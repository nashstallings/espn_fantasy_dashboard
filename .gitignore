# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/

# Environment / secrets — never commit ESPN cookies or keys
.env
.env.*
!.env.example
*-key.json
service-account*.json

# Editors / OS
.DS_Store
.idea/
.vscode/
*.swp

# Built league data. Generated by the scheduled workflow and uploaded straight
# into the Pages artifact — never committed, because this repository is public
# and committed league data would sit in git history permanently.
frontend/data/

# Node (frontend has no build step today; keep this for future tooling)
node_modules/

# Downloaded nflverse source data for analysis/. Regenerated on demand and
# large enough that it does not belong in git; the derived CSVs are committed.
analysis/.cache/
